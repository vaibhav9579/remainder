package com.remainder.app.ui.theme

import androidx.compose.material3.Typography

// Placeholder Material 3 typography using the platform default (Roboto).
// The full Remainder type hierarchy is defined in Phase 1 (design system).
val RemainderTypography = Typography()
